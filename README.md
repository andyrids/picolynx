# Serpent

```sh
textual console -x DEBUG -x EVENT -x INFO -x SYSTEM
```

```sh
textual run --dev src/serpent/__main__.py
```

    & > .datatable--odd-row {
        background: #131a2c;
    }
    & > .datatable--even-row {
        background: #0f1525;
    }